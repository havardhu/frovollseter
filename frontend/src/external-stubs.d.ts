// Intentionally empty. The real types ship with `quill` (^2.0.3) and `dompurify`
// (^3.2.4) once `npm install` is run. This file used to hold stubs needed for
// typecheck before those packages were available locally.
export {};
